# :heart_decoration: ${{\color{#dcb4bc}\{\textsf{C\ SHARP\ EĞİTİM\ KAMPI\ FİNANSAL\ CRM\ UYGULAMASI\ \}}}}\$:cherry_blossom:
##  :seedling: Murat Yücedağ'ın başlatmış olduğu C# eğitim kampı kapsamında gerçekleştirdiğimiz finalsal crm uygulaması ile ilgili bilgi ve öğrendiklerimi içermektedir.Yaptığım uygulamayla öğrendiklerimi pekiştirme ve deneyimleme şansını elde ettim. Her adım yeni bilgileri keşfetmemde ve kedimi daha üst noktaya taşımamda etkili oldu. Veritabanı işlemlerimden, tablo tasarımına ve kullandığımız bir çok metodun ve yaptığımız uygulamanın detaylı ve teknik açıklaması aşağıda görülen bölümlerde mevcuttur: :four_leaf_clover:
## 	:ribbon:1. BÖLÜM: SQL VERİTABANI, ENTİTY FRAMEWORK DBFİRST VE MODEL OLUŞTURMA
### Sqlde tablolarımızı oluşturduk. Windows Form kullanarak yapacağımız finansal crm projesi için Users, Categories , Banks, Bills, BankProcesses, Spendings tablolarımızı oluşturduk.Tablolarımızın ilişkilendirmesini yaptık.Bankalarla banka hareketlerini, kategorilerle giderleri ilişkilendirdik. (1->∞) Veritabanını projeye entegre etmek için dabase first yöntemini kullandık. Database first var olan veritabanını enitity framework ile nesneleri, classlarını otomatik olarak bir modelle oluşturur. :bouquet:
## :open_umbrella:2. BÖLÜM: LİNQ SORGULARI, DB NESNESİ, BANKA FORMU
### Ana menü ve butonlarımız için bir panel oluşturdum. Bu butonlarımın olduğu ana menüyü her forma yerleştirdim. Banka bakiyelerini göstermek için ayrı paneller koydum. Son 5 banka hareketini gösterdiğimiz bir grupbox oluşturdum. Panel , groupbox ve label nesnelerini kullandık. Panel ve groupbox BackColorına dışardan renk atadım. Veritabanından bilgileri alabilmek için bir db nesnesi türettim. Formun yüklendiğinde devreye girmesi için formun loadında kodlarımızı yazdım. Her bir (ziraat, akbank, iş bankası) bankalarımızın bakiyelerini db nesnesi ile isimlerini where linq sorgusu ile karşılaştırarak tek veri olarak bakiyelerini değikenlere aktardım. her bir bankanın bakiyesini ayrı panellerdeki labellara aktarıp göstermiş oldum.
### Son 5 banka hareketini göstermek için bir sıralama yapmam gerekiyor. Son 5 veri için z-aya sırala yaptıracağız. Z-aya sıralama için OrderByDescending() kullanılır. db nesnesiyle banka hareketleri tablosuna ulaşarak OrderByDescending ile idye göre Take(1) ifadesi ile birinciyi alıyorum ve değişkenime aktarıyorum. Aldığım verinin açıklama , tutar ve tarihini labelda gösteriyorum. İkinci banka hareketinde değişen Take(2).Skip(1) ile bir taneyi atlayıp öyle getirecek. Her seferinde bu sayılar artarak devam ediyor. 3.de ise 2 tane atlayıp 3. veriyi getirecek. Take(3).Skip(2) Hepsini tek tek groupboxımızdaki labellarda gösterdim. var bankProcess4 = db.BankProcesses.OrderByDescending(x => x.BankProcessId).Take(4).Skip(3).FirstOrDefault();
## :scroll:3. BÖLÜM: ÖDEME & FATURA FORMU
### 3.1 Tasarımında panel, texBox, groupBox, button, dataGridView kullandığım ve fatura verilerinin (ödeme id, başlık, miktar, periyot) ekle sil güncelle işlemlerini yapacağımız bir ödeme&fatura formu oluturdurdum. Db nesnesini tanımladım. Formun loadında db nesnesi ile faturalar(Bills) tablosuna ulaşarak verilerimi ToList() metodu ile isteleyip bir değişkene atadım. Atadığım verileri dataGridView nesnemin içine atarak gösterdim. Aynı şekilde listele butonunun içine de listeleme yapan kodumu yazdırdım. 
### $${\color{green}3.2 \space EKLE:}$$ Ekleme yapmak için Ekle butonun click ine (butona basınca gerçekleşecek) ekleme kodlarımı yazdım. Texboxlardan aldığım string, amount, periyot verileri mi tek tek dönüştürüp kendi türündeki her bir değişkene atadım. Bills sınıfından bir nesne oluşturdum. Texboxdan aldığın verileri bu nesnemdeki sütunlarına aktardım. Db nesnesi ile Bills tablosuna ulaşarak bu verileri eklemedim ve kaydettim. Ekleme db.Bills.Add(bills); kullanılır. Kaydetme işlemi için db.SaveChanges(); kullanılır. Ekleme işleminin yapıldığına dair MessageBox ile bir mesaj verdim.
### $${\color{red}3.3 \space SİL:}$$ Silme işlemi için silme butonunun içine silme kodlarımı yazdım. TexBoxdan aldığım idyi find metodu ile bulup bir değişkene atadım. Sonra bulduğum veriyi silmek için Remove() metodunu kullandım. Db nesnesi ile tabloya ulaşıp idnin bulunduğu veri sildirdim ve işlemi kaydettim. db.Bills.Remove(removeValue); MessageBox.Show() ile Silme işlemine dair bir mesaj verdim. Tekrardan verilerimi listelettim.
### $${\color{blue}3.4 \space GÜNCELLE:}$$ Güncelleme işlemi için güncelleme butonunun içine güncelleme kodlarını yazdım. TexBoxdan aldığım verileri kendi türündeki değişkenlere atadım. idyi değişkene atarak Find() metodu ile veriyi buldum. Texboxlardan alıp değişkine atadığım verileri bulduğum verinin ilgili sütunlarına aktardım ve değişiklikleri kaydettim. MessageBox.Show() ile güncelleme işeminin başarılı olduğuna dair bir mesaj verdim. Tekrardan verilerimi listelettim.
## :bookmark_tabs:4. BÖLÜM: KATEGORİLER, GİDERLER, BANKA HAREKETLERİ FORMU
### 4.1 Tasarımında panel, texBox, groupBox, button,DateTimePicker, ComboBox, dataGridView kullandığım kategori , gider ve banka hareketleri formu oluşturdum. Db nesnesini tanımladım. Formun loadında db nesnesi ile kategoriler, giderler ve banka hareketleri tablolarının her birine ayrı ayrı ulaşarak verilerimi ToList() metodu ile isteleyip bir değişkene atadım. Atadığım verileri dataGridView nesnemin içine atarak gösterdim. Aynı şekilde listele butonunun içine de listeleme yapan kodumu yazdırdım. Faturlardaki işlemlerin aynısını bu tablolara da uyguladım.
### $${\color{green}4.2 \space EKLE:}$$ Ekleme yapmak için Ekle butonun click ine (butona basınca gerçekleşecek) ekleme kodlarımı yazdım. Texboxlardan aldığım kategori için kategori adı, giderler için id, başlık, miktar, tarih, kategori ve banka hareketleri için id, açıklama, miktar, işlem türü, tarih, banka verilerimi tek tek dönüştürüp kendi türündeki her bir değişkene atadım. Her sınıf için kendi sınıfından bir nesne oluşturdum. Texboxdan aldığın verileri bu nesnemdeki sütunlarına aktardım. Db nesnesi ile her formun kendi tablosuna ulaşarak(Categories, Spendings, BankProcesses) bu verileri eklemedim ve kaydettim. Ekleme db.Bills.Add(Categories); kullanılır. Kaydetme işlemi için db.SaveChanges(); kullanılır. Ekleme işleminin yapıldığına dair MessageBox ile bir mesaj verdim. TexBoxları Clear() metodu ile temizledim. Verilerimi tekrardan listelettim.
### $${\color{red}4.3 \space SİL:}$$ Silme işlemi için silme butonunun içine silme kodlarımı yazdım. TexBoxdan aldığım idyi find metodu ile bulup bir değişkene atadım. Sonra bulduğum veriyi silmek için Remove() metodunu kullandım. Db nesnesi ile tabloya ulaşıp idnin bulunduğu veriyi sildirdim ve işlemi kaydettim. db.Categories.Remove(removeValue); MessageBox.Show() ile Silme işlemine dair bir mesaj verdim. TexBoxları Clear() metodu ile temizledim. Tekrardan verilerimi listelettim.
### $${\color{blue}4.4 \space GÜNCELLE:}$$ Güncelleme işlemi için güncelleme butonunun içine güncelleme kodlarını yazdım. TexBoxdan aldığım verileri kendi türündeki değişkenlere atadım. int veriyi int.Parse() ile tarihi DateTime.Parse() ile dönüştürerek atadım. idyi değişkene atarak Find() metodu ile veriyi buldum. Texboxlardan alıp değişkine atadığım verileri bulduğum verinin ilgili sütunlarına aktardım ve değişiklikleri kaydettim. MessageBox.Show() ile güncelleme işeminin başarılı olduğuna dair bir mesaj verdim. TexBoxları Clear() metodu ile temizledim. Tekrardan verilerimi listelettim.

## :space_invader: 5. BÖLÜM: COMBOBOX KULLANIMI
### Comboboxa verileri çekmek için formun loadına kodlarımı yazdım. Giderlerideki kategorileri, banka hareketlerinde bankaları getirmek için comboBox kulladım. Giderlerdeki kategoriler için : db nesnesi Categories tablosuna erişerek verilerimi değişkene ToList() metodu ile listelettim. Daha sonra comboBoxın DisplayMember a CategoryName i , ValueMember ise CategoryId atadık. DisplayMember ile comboBox içerisinde gözükecek olan alan bilgisini belirtiyoruz. ValueMember ilede seçilen satırın arka planda hangi değeri tutacağı belirtilmektedir. ComboBoxın DataSource'una kategoriden aldığımız verileri attım. Bu şekilde kategorileri comboBoxa getirmiş oldum. Banka Hareketlerindeki bankalar için : db nesnesi ile Banks tablosuna erişip verileri çekiyorum. comboBoxın DisplayMember'a BankTitle , ValueMember ise BankId atadım. ComboBoxın DataSource'una bankalardan çektiğimiz verileri attım. Bu şekilde bankaları comboBoxa getirmiş oldum.
## 	:hibiscus: 6. BÖLÜM:DASHBOARD FORMU
### Tasarımında panel, texBox, label, groupBox, button, chart, timer kullandığım bir dashboard oluşturdum. Db nesnesini tanımladım. Formun loadında db nesnesi ile bankalar(Banks) tablosuna ulaşarak toplam bakiye mikatarını bulmak için bakiyeleri Sum() metodu ile toplayıp bir değişkene atadım. Panelimin içine yerleştirdiğim labelımda toplam bakiyeyi gösterdim. Gelen son havaleyi diğer panelimdeki labelıma yazdırmak için db nesnesi ile banka hareketleri(BankProcesses) tablosuna erişerek z-a'ya idye göre sıralama yaptırıp Take(1) ile son veriyi aldım. Tek verilerde hep FirstOrDefault() kullandım.
### Chart 1 : Chartın Series ismini değiştirmek için Add() metodunu kullandım. chart1.Series.Add("Series1"). bankData değişkenini tanımlayıp içine banka başlığı ve bakiyesi sıralayan linq sorgusu yazıp değişkenime listeledim. Foreach döngüsü ile item tanımlayıp bankData içindeki verileri sırasıyla Chart1'in içine yazdırdım. X ine Ysine döngüde başlık ve bakiyeleri atadım. Yazdırırken series.Points.AddXY(item.BankTitle, item.BankBalance); metodunu kullandım.
### Chart 2 : Chartın Series ismini değiştirmek için Add() metodunu kullandım. chart2.Series.Add("Faturalar"). ChartType değiştirmek için SeriesChartType.FastLine; uyguladım. billData değişkenini tanımlayıp içine fatura ve tutarını sıralayan linq sorgusu yazıp değişkenime listeledim. Foreach döngüsü ile item tanımlayıp billData içindeki verileri sırasıyla Chart2'in içine yazdırdım. X ine Ysine döngüde fatura ve tutarını atadım. Yazdırırken series.Points.AddXY(item.BankTitle, item.BankBalance); metodunu kullandım.
![image](https://github.com/user-attachments/assets/d8c2add7-db19-4ba0-a3da-44c87448b32a)
### :hourglass:Timer : count=0; değişkeni tanımladım. Forma bir timer ekledim. Belirli aralıklarda tetiklenmesi için ilgili kodlarımı Timer'ın Tick olayına yazıyorum. if koşulu ile her count arttığında gösterdiğimiz fatura değeri değişecek. Count değerini her seferinde bir artırıyorum. Eğer count%4 ==1 ise elektrik faturasını gösterecek. count arttıkça sırasıyla count % 4 == 2 Doğalgaz, count % 4 == 3 Su, count % 4 == 0 İnternet faturasını gösterecek. Her bir koşula göre o fatura değerine ulaşıp labelda gösteriyoruz.

## :crystal_ball: 7. BÖLÜM:İSTATİSTİKLER FORMU
### Chart kullanarak verilere genel bakış için bir istatistik formu oluşturdum. Giderler ve maliyeti , banka hareketleri ve havale tutarı, kategorilerilere göre gider maaliyet dağılımı, banka haraketlerinde aylara göre toplam tutar dağılımı olarak dört tane grafik ekledim. Formun loadına chart kodlarımın hepsini linq sorgularım ile verileri çekip içine yazdırdım.
### $${\color{MediumPurple}7.1 \space CHART 1:}$$ Giderler ve maaliyetleri için db nesnesi ile Spendings tablosuna erişip başlık ve maaliyeti spendingData değişkenime aktardım. chart1.Series.Clear(); yapıp chartımı temizledim. Add() ile series ismini Giderler yaptım. Foreach döngüsü ile series.Points.AddXY() kullanarak SpendingTitle ve SpendingAmount chartımın eksenlerine işledim.
### $${\color{orange}7.2 \space CHART 2:}$$ Banka Hareketleri ve bakiyeleri için db nesnesi ile BankProcesses tablosuna erişip açıklama ve bakiyeyi bankProcesses değişkenime aktardım. chart1.Series.Clear(); yapıp chartımı temizledim. Add() ile series ismini Giderler yaptım. Foreach döngüsü ile series.Points.AddXY() kullanarak SpendingTitle ve SpendingAmount chartımın eksenlerine işledim.
### :chart: $${\color{turquoise}7.3 \space CHART 3:}$$ Kategorilere göre gider toplam gider maaliyetini grafiğe dökmek için kategoriye göre gruplama yaparak verileri seçtim. db nesnesi ile Spendings tablosuna erişerek GroupBy(x => x.Categories.CategoryName) ile kategori isminine göre gruplayarak kategori ismini ve Sum() ile hesaplayıp gider maaliyetinin toplamını getirdiğim sorgumu değişkenime aktardım. Buradan gelen kategori bilgisini ve toplam giderleri Doughnut grafiğine döktüm, oran hesaplaması yaptırmış oldum. chart3.Series.Clear(); yapıp chartımı temizledim. Foreach döngüsü ile series.Points.AddXY() kullanarak CategoryName ve SpendingAmount chartımın eksenlerine yerleştirdim.
### $${\color{yellowgreen}7.4 \space CHART 4:}$$ Banka Hareketlerinde havalelerin aylara göre miktarını gösteren bir piramit grafiği oluşturdum. GroupBy() ile birlikte yıla ve aya göre gruplama yaptım. Sum() birlikte toplam havale tutarlarınıda aldım.Ayları türkçe olarak almak için sqlden ay ve yıl verileri aldıktan sonra .AsEnumerable() ile belleğe alıp işledim. ToString("MMMM", new CultureInfo("tr-TR")) kullanarak ay adını ekledim. .OrderBy(y => y.processYear).ThenBy(y => y.processMonth) ile kronolojik sıralama yaptırdım. hart3.Series.Clear(); yapıp chartımı temizledim.SeriesChartType.Pyramid ile piramit tipinde bir chart kullandım. Foreach döngüsü ile series.Points.AddXY() kullanarak monthName ve Amount ile sorgumdan aldığım verilerimi tek tek chartımın eksenlerine yerleştirdim.
![image](https://github.com/user-attachments/assets/d11efd47-c190-4e38-9a09-664cd372f1f9)

## :grapes: 8. BÖLÜM: ADMİN FORMU
### Formumda label, texBox, Button, checkBox nesnelerimi kullandım. Giriş yapmak için btnEnter buton kullandım. bntEnter Click içine kullanıcı adı ve parola kontrolünü yapan kodlarımı yazdım. Texboxlarımdan kullanıcı adını ve şifresini alıp değişkenlere attım. db nesnesi ile Users tablosuna ulaştım idsi 1 olan kullanıcı adı bilgisini bir değişkene yine idsi 1 olan parola bilgisini başka değişkene atadım. if ile birlikte texBoxlardan aldığım kullanıcı adı ve parolayla veritabanından çektiğim aynı ise uygulamama giriş yaptırdım. İkisinden birisi veya ikisi de yanlış ise MessageBox.Show() ile bir hata mesajı verdim. Parolayı gizledim. Formun loadında PasswordChar * şeklinde görünmesini sağladım. İstedğim zaman parolayı görüp gizlemek için bir checkBox kontrolü gerçekleştirdim. checkBox nesnemin CheckedChanged içine if kontrolü yazdırdım. Eğer checkBox işaretlenmişse parolayı göster değilse gizle şeklinde if else koşulu yazdım.
![image](https://github.com/user-attachments/assets/8d26b7bc-a7e1-4773-bd6d-10c198dfcc3d)

## :oyster: 9. BÖLÜM: FORMLARI BİRBİRİNE BAĞLAMAK
### Dashboarddan kategoriye kategori butonuna basıp gitmesi için btnCategories Click içine FrmCategoriesten yani kategori formundan bir nesne türetiyorum. Show() diyerek diğer formumu göstermiş oldumç this.Hide() veya this.Close() kullanarak esas formumu gizleyebilir veya kapatabilirim. Bütün ana menü formlarını bu şekilde birbirine bağladım. Olduğum formun butonunun Enabled özelliğini false yaparak butonu devre dışı bıraktım. Hangi formda olduğum anlaşılır olmuş oldu. 
## 	:love_hotel: 10. BÖLÜM: DİĞER FORM GÖRSELLERİ
### KATEGORİ FORMU
![image](https://github.com/user-attachments/assets/48130157-7229-4a51-8713-f33dbbdab5aa)
### BANKA FORMU
![image](https://github.com/user-attachments/assets/03f9e9e1-0b19-4706-accb-9622b59e66a4)
### ÖDEME & FATURALAR FORMU
![image](https://github.com/user-attachments/assets/32ded7ad-7030-426f-8913-d85110add759)
### GİDERLER
![image](https://github.com/user-attachments/assets/bd3ece9d-b3e2-4563-9654-7bb08f1f11bb)

### BANKA HAREKETLERİ
![image](https://github.com/user-attachments/assets/4802bc46-1a60-4d5c-a1f1-41801c202129)
